# Exact versions the test suite last passed against. Install with
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
# pyproject.toml holds the minimum supported versions for the same packages.

# Runtime
fastapi==0.129.0
starlette==0.52.1
uvicorn==0.41.0
websockets==16.0
anyio==4.12.1
jinja2==3.1.6
python-multipart==0.0.22
aiosqlite==0.22.1
pydantic==2.12.5
python-dotenv==1.2.1
bcrypt==5.0.0

# Tests
pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1
